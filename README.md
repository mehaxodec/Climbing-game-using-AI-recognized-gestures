# Climbing-game-using-AI-recognized-gestures
This is a prototype for the application of AI-recognized gestures, it's a game and use it as you like

instruction on how to set up this mess
run on python .12 or .11 idk but just not .14

paste this into your cmd if you don't know what to do
"""
python -m pip install --upgrade pip
pip install opencv-python
pip install numpy
pip install mediapipe
pip install pygame
"""
(don't copy the " dummy)

if any error occurs (or you are lazy to downgrade your python version paste this)
"""
pip install mediapipe --pre
"""
